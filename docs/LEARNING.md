## learning of 25th/5
- .env is different from .env.example as it is just blueprint
- DX is developer experience
- dockerfile is build one container, and docker-compose.yml is how to run multiple container together
- npm.cmd is windows batch wrapper file


## learning of 26th/5(spent 5 hours to resolve this)
- recreate the volume if connetion error comes again and again
- if that not solve the proble then change your password, port, url, and run again(python -m uvicorn backend.app.main:app --reload)
- see error direct in connection of db with psycopy.Error as e
and print e.
